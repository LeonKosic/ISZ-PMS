import { Button } from "@suid/material"

export default function ApproveRequestBtn(props) {
  return (

    <Button variant="outlined"
        class="w-auto"
        color="pmsScheme"
      >
        Approve
    </Button>
  )
}