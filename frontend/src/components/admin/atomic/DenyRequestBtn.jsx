import { Button } from "@suid/material"

export default function DenyRequestBtn(props) {
  return (
    <Button variant="outlined"
        class="w-auto"
        color="pmsScheme"
      >
        Deny
    </Button>
  )
}